# ─── Serverless Runtime (NOT needed: fastapi, uvicorn) ───────────────────────
runpod>=1.14.0

# ─── Coqui XTTS v2 TTS Engine ────────────────────────────────────────────────
# Requires torch>=2.4 (installed separately in Dockerfile with CUDA 12.4)
coqui-tts>=0.27.0
coqpit-config
transformers>=4.40.0
tokenizers>=0.19.0

# ─── Text Processing ──────────────────────────────────────────────────────────
tqdm==4.66.1
unidecode==1.3.7
packaging>=23.0
nest_asyncio==1.5.8

# ─── Document Parsing (PDF / EPUB / TXT) ─────────────────────────────────────
pypdf==5.1.0
ebooklib==0.18
beautifulsoup4==4.12.3
lxml==5.3.0

# ─── Cloud Storage (S3 / Cloudflare R2) ──────────────────────────────────────
boto3>=1.34.0

# ─── HTTP Utilities ───────────────────────────────────────────────────────────
requests>=2.31.0
